# Active execution plans

Read only the plan linked from the selected `docs/BACKLOG.md` task. Each plan is
an execution and acceptance contract, not a session transcript.

At most one active writer owns a mutable artifact at a time. Update progress,
decisions, blockers, and final evidence during material work; move accepted
plans to `../completed/`.
