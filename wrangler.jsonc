// Deploys the landing site (site/) as a Cloudflare Worker with static assets.
// `custom_domain: true` auto-creates the rmdv.mclee.dev DNS record on deploy.
{
  "name": "rmdv-site",
  "compatibility_date": "2026-06-12",
  "assets": {
    "directory": "./site"
  },
  "routes": [
    { "pattern": "rmdv.mclee.dev", "custom_domain": true }
  ]
}
